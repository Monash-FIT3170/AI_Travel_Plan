import type { AnySource, CustomSource } from '../types';
export declare type SourceProps = (AnySource | CustomSource) & {
    id?: string;
    children?: any;
};
declare function Source(props: SourceProps): any;
export default Source;
