import React from "react";

function AddButton() {
  const handleClick = () => {
    // Add popup here
  };

  return <button onClick={handleClick}>Add destination</button>;
}

export default AddButton;
