import "../style.css";
import React from "react";

function EditButton() {
  const handleClick = () => {
    // Add popup here
  };

  return <button onClick={handleClick}>Edit destination</button>;
}

export default EditButton;
